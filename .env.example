# Production / Vercel — copy to Vercel → Settings → Environment Variables
VITE_API_URL=https://opticloud-server-wh26.onrender.com/api

# Local dev with local API server — use .env.development instead (VITE_API_URL=/api)
